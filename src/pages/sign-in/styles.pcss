.logoContainer {
  position: absolute;
  top: 40px;
}

.mainWrapper {
  width: 100%;
  padding: 0 320px;
}

.title {
  color: var(--light);
  font-size: 24px;
  line-height: 28px;
  font-weight: 600;
  margin: 0 0 25px 0;
}

.signUpOption {
  padding: 20px 0;
  width: 100%;
  display: flex;
  justify-content: flex-start;
  align-items: center;

  .text {
    color: var(--light);
    font-size: 14px;
    line-height: 16px;
    margin-right: 5px;
  }
}
